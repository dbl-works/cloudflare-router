{
  "name": "cloudflare-router",
  "version": "0.2.10",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "jest test/*.test.ts test/**/*.test.ts --coverage"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.0.0",
    "@types/jest": "^27.0.2",
    "@types/node-fetch": "^3.0.3",
    "browser-cache-mock": "^0.1.7",
    "jest": "^27.3.0",
    "jest-fetch-mock": "^3.0.3",
    "ts-jest": "^27.0.7",
    "ts-loader": "^9.2.6",
    "typescript": "^4.4.4"
  }
}
